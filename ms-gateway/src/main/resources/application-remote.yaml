eureka:
  client:
    service-url:
      defaultZone: http://server:8761/eureka

server:
  ssl:
    enabled: ${SSL_ENABLED:true}
    certificate: /etc/ssl/mmedalist.crt
    certificate-private-key: /etc/ssl/mmedalist.key
    trust-certificate: /etc/ssl/ca.crt