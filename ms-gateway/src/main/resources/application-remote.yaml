eureka:
  client:
    service-url:
      defaultZone: http://server:8761/eureka

server:
  ssl:
    enabled: ${SSL_ENABLED:false}
    certificate: /etc/ssl/medalists.crt
    certificate-private-key: /etc/ssl/medalists.key
    trust-certificate: /etc/ssl/ca.crt